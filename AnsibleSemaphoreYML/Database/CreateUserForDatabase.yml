---
- name: Create User Using Semaphore Vault Secrets
  hosts: all
  become: true

  tasks:
    - name: Fetch username from Semaphore Vault
      set_fact:
        user_name: "{{ lookup('semaphore', 'USER_NAME') }}"

    - name: Fetch password from Semaphore Vault
      set_fact:
        user_password: "{{ lookup('semaphore', 'USER_PASSWORD') | password_hash('sha512') }}"

    - name: Create new user with Semaphore Vault credentials
      user:
        name: "{{ user_name }}"
        password: "{{ user_password }}"
        shell: /bin/bash
        create_home: yes

    - name: Confirm user creation
      debug:
        msg: "User {{ user_name }} has been created successfully."
